#include "main.h"
/**
 * _abs - the absolute value of an integer
 * @n: the tested integer
 *
 * Return: n if positive and -n if negative
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
