#include "main.h"
#include<stdio.h>
/**
 * _isupper - test if a char c is upper case or no
 *@c: char
 * Return: 1  if c is upper 0 if not
 */
int _isdigit(char c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
}
